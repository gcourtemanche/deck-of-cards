import styled from 'styled-components';

import * as styledComponents from 'styledComponents/';

export const Wrapper = styled.div`
  text-align: center;
`;

export const Button = styled(styledComponents.Button)`
  text-transform: uppercase;
`;
